# CLAIM AUDIT TABLE — SCIENTIFIC DEFENSE & CORRECTIONS

| No. | Original / Vulnerable Claim | Audited & Corrected Journal Claim | Scientific Grounding & Evidence | Status |
| :---: | :--- | :--- | :--- | :---: |
| 1 | "HEDO is a strictly contractive, dissipative dynamical system with guaranteed bounded discrete Hamiltonian energy." | "HEDO is a Hamiltonian-inspired discrete dissipative coordinate–momentum transformation with positive damping ($\beta=0.05$)." | Pre-training diagnostic demonstrates energy modulation ($\mathcal{H}_{\text{raw}} = 170.82 \to \mathcal{H}_{\text{HEDO}} = 77.56$). Discrete trajectory is not monotonically decreasing ($170.82 \to 174.38$), so no universal Lyapunov theorem is claimed. | 🟢 **PASS** |
| 2 | "Full HEDO-HVSC significantly outperforms the baseline, establishing state-of-the-art results on Flickr8k." | "The full HEDO-HVSC architecture achieves retrieval performance comparable to the controlled SSD baseline ($48.28\% \pm 0.59\%$ vs. $48.25\% \pm 0.47\%$, mean paired delta $+0.03\% \pm 0.20\%$). | Authoritative 12-run multi-seed benchmark (`master_results.csv`) on official held-out test split ($N=1{,}000$ images, $5{,}000$ captions). | 🟢 **PASS** |
| 3 | "Employs native hardware-optimized Mamba-2 / official CUDA kernel recurrence." | "Employs a custom PyTorch SSD-style recurrent state-space block with state continuity across chunk boundaries ($C=16, d_{\text{state}}=64$)." | Pure PyTorch implementation with sequential chunk scanning, avoiding misleading CUDA kernel claims. | 🟢 **PASS** |
| 4 | "Proves exact linear complexity $\mathcal{O}(L)$ via linear regression ($R^2 = 0.9998$)." | "Recurrence performs an algorithmically linear number of state updates $\mathcal{O}(L)$, exhibiting approximately linear empirical latency scaling over $L \in [16, 256]$." | $R^2 = 0.9998$ is cited as a regression goodness-of-fit metric over empirical latency data ($2.89\text{ ms}$ to $38.60\text{ ms}$), not as mathematical asymptotic proof. | 🟢 **PASS** |
| 5 | "HEDO-HVSC demonstrates superior and universal robustness across all input corruptions." | "Robustness evaluation exhibits a mixed profile, outperforming the baseline by $+0.50\%$ under severe Gaussian noise ($\sigma=0.10$), while results under other conditions are mixed." | Secondary stress test on 100-image test subset: Clean ($-0.87\%$), Gaussian $\sigma=0.05$ ($-0.47\%$), Gaussian $\sigma=0.10$ ($+0.50\%$), Brightness $+30\%$ ($-0.60\%$). | 🟢 **PASS** |
| 6 | "HVSC eliminates padding distortion and bounds mutual information via an exact Information Bottleneck." | "HVSC performs mask-weighted boundary-state aggregation to reduce padding-induced aggregation bias, regularizing Gaussian latents via symmetric KL divergence." | No unsupported mutual information bounds claimed; strictly describes mask-weighted boundary pooling and FP32 symmetric KL regularization. | 🟢 **PASS** |
| 7 | "Cross-modal variational state coupling enables bidirectional feedback between modalities." | "Cross-modal alignment is enforced during training via symmetric KL divergence; test-time retrieval evaluates deterministic posterior means strictly unimodally." | Test retrieval computes $\hat{\mathbf{e}}_I = f(\mathbf{I})$ and $\hat{\mathbf{e}}_T = g(\mathbf{T})$ independently with zero cross-attention or cross-modal communication. | 🟢 **PASS** |
| 8 | "HEDO and HVSC act orthogonally within the architecture." | "The estimated $2 \times 2$ factorial interaction effect is small ($-0.12\%$), suggesting limited interaction between the two components under the present benchmark." | Interaction term $\Delta_{\text{int}} = -0.12$ percentage points across 3 seeds. | 🟢 **PASS** |
